# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.env
# Files related to Supabase configuration/keys (as per your request)
WM_IO_my_rsa_key_pair
WM_IO_private_key.pem
WM_IO_private_key.prm
WM_IO_public_key.pem

# Recommended: General Supabase environment variables files
# It's a best practice to store your Supabase URL, anon key, service_role key,
# JWT secret, and other sensitive credentials in .env files.
# Ensure these .env files are listed in .gitignore.
.env
*.env
.env.*
supabase/.env
supabase/functions/.env
